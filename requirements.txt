# Agent SDK Dependencies (two options supported)
# Using OpenAI Agents SDK
openai-agents>=0.5.0                  # OpenAI Agents SDK (standalone package)
openai>=1.55.0                        # OpenAI API client (dependency of Agents SDK)

# Using Claude Agent SDK (optional)
# claude-agent-sdk>=0.1.3             # Claude API client (requires GitHub installation)
# Note: claude-agent-sdk is not published to PyPI yet, manual installation required:
# pip install git+https://github.com/anthropics/anthropic-sdk-python.git

# Code Parsing and Analysis
tree-sitter-language-pack>=0.10.0    # 100+ language parsers (pre-compiled wheels, maintained fork)
tree-sitter>=0.23.0                   # Tree-sitter Python bindings

# File Type Detection
python-magic>=0.4.27             # File type detection via magic bytes
chardet>=5.2.0                   # Character encoding detection

# File System and Pattern Matching
pathspec>=0.12.1                 # .gitignore pattern matching

# Graph and Data Analysis
networkx>=3.1                    # Dependency graph construction
matplotlib>=3.8.0                # Graph visualization

# Utilities
rich>=13.7.0                     # Beautiful terminal output
pydantic>=2.5.0                  # Data validation
typing-extensions>=4.9.0         # Type hints support
python-dotenv>=1.0.0             # .env file support for configuration

# Development Dependencies
pytest>=7.4.3                    # Testing framework
pytest-asyncio>=0.21.1           # Async test support
black>=23.12.0                   # Code formatting
mypy>=1.7.0                      # Type checking
